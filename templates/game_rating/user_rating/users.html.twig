{% extends 'base.html.twig' %}


{% block body %}
    <div class="col-md-12">
        <div class="card">
            <div class="card-header text-center">
                <h5>{{ 'dashboard.rankList'|trans }}</h5>
            </div>

            <div class="card-body">

                <div class="row">
                    <div class="col-md-12">
                        <div class="table-responsive">
                            <table class="table">
                                <thead class="">
                                <tr>
                                    <th>
                                        {{ 'dashboard.ranking.position'|trans }}
                                    </th>
                                    <th>
                                        {{ 'login.username'|trans }}
                                    </th>
                                    <th>
                                        r. {{ 'dashboard.result'|trans }}
                                    </th>
                                    <th>
                                        r. {{ 'dashboard.ranking.difference'|trans }}
                                    </th>
                                    <th>
                                        r. {{ 'dashboard.ranking.winner'|trans }}
                                    </th>
                                    <th>
                                        {{ 'dashboard.ranking.nohit'|trans }}
                                    </th>
                                    <th class="text-left">
                                        {{ 'dashboard.points'|trans }}
                                    </th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for userScoreWithPosition in userScoreWithPositions %}
                                    <tr{% if myUserId == userScoreWithPosition.userScore.user.id %} class="table-secondary"{% endif %}>
                                        <td>
                                            {{ userScoreWithPosition.position }}
                                        </td>
                                        <td>
                                            {% if myUserId == userScoreWithPosition.userScore.user.id %}
                                                {{ userScoreWithPosition.userScore.user.username }}
                                            {% else %}
                                                <a class="text-danger" href="{{ path('game_past_games_by_users', {'userId': userScoreWithPosition.userScore.user.id}) }}">
                                                    {{ userScoreWithPosition.userScore.user.username }}
                                                </a>
                                            {% endif %}

                                        </td>
                                        <td>
                                            {{ userScoreWithPosition.userScore.countWinExact }}
                                        </td>
                                        <td>
                                            {{ userScoreWithPosition.userScore.countWinScoreDiff }}
                                        </td>
                                        <td>
                                            {{ userScoreWithPosition.userScore.countWinTeam }}
                                        </td>
                                        <td>
                                            {{ userScoreWithPosition.userScore.countNoWinTeam }}
                                        </td>
                                        <td class="text-left">
                                            {{ userScoreWithPosition.userScore.scoreSumme }}
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>

            </div>

        </div>
    </div>
{% endblock %}